"""
Домашняя работа: Анализ сотрудников компании

### Условие:

Дан DataFrame с данными о сотрудниках:

```python
import pandas as pd

data = {
    'Name': ['Alice', 'Bob', 'Charlie', 'Diana', 'Ethan', 'Fiona', 'George', 'Hannah'],
    'Department': ['HR', 'IT', 'IT', 'HR', 'Finance', 'Finance', 'IT', 'HR'],
    'Salary': [3000, 4000, 4500, 3200, 5000, 4800, 4200, 3100],
    'Experience (Years)': [2, 5, 4, 3, 7, 6, 5, 2]
}

df = pd.DataFrame(data)
```

---

### ✅ Задания:

1. **Выведите максимальную зарплату в каждом отделе.**
   📌 *Подсказка: используйте `groupby` и `max()`.*

2. **Посчитайте количество сотрудников в каждом отделе.**
   📌 *Подсказка: `groupby` и `count()` по столбцу `Name`.*

3. **Найдите отдел с самой высокой средней зарплатой.**
   📌 *Подсказка: используйте `groupby` и `mean()` или `pivot_table`.*

4. **Добавьте новый столбец `Salary per Year`,**
   который показывает среднюю зарплату за 1 год опыта работы.
   📌 *Подсказка: деление `Salary` на `Experience (Years)`.*

5. **Выведите имя сотрудника с самой высокой `Salary per Year`.**

6. **Отсортируйте сотрудников по зарплате по убыванию.**

7. **Выведите только тех сотрудников, у кого зарплата выше средней по всей компании.**

---

### ⭐ Дополнительно (по желанию):

8. **Найдите среднюю зарплату по каждому отделу и выведите её в виде отдельной таблицы.**
9. **Посчитайте общее количество лет опыта у всех сотрудников в отделе IT.**
10. **Выведите 3 сотрудников с самым низким уровнем `Salary per Year`.**
"""

# Ваш код ..